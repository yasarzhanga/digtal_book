/goal 在当前仓库中一次性完成“数字教材平台沉浸式展示 Demo V2”。开始前完整阅读并严格执行根目录 `AGENTS.md`、`PRODUCT_SPEC.md`、`ARCHITECTURE.md`、`ACCEPTANCE_MATRIX.md`、`DEMO_STORYBOARD.md` 和 `seed/lesson-blueprint.json`；仓库为空则自行初始化。任务包中的 `starter-assets` 是必须使用的本地音频、视频、3D、全景、PDF、图片和 DOCX 示例素材。

持续工作到 P0 全部通过，不要停在计划、脚手架、静态 UI 或部分组件。产品重点是：真正的 TipTap 富文本编辑器；图片热点、画廊、音频、视频、公式、交互图表、F=ma 仿真、3D、360 全景、知识气泡、扩展阅读、PDF、四类题型、录音任务和知识图谱；同一内容引擎的编辑/预览/阅读；传统教材与数字教材同位置对比；真实学习行为、教师同步、随堂题、签到和数据统计。不得使用外部密钥、远程示例资源、假按钮、硬编码图表或仅存在前端内存的核心状态。

维护简短 `PROGRESS.md`，按 `ACCEPTANCE_MATRIX.md` 编号推进。先完成共享内容引擎和发布链路，再逐个完成富媒体纵向切片，最后完成阅读、追踪、教学、响应式与验证。每个检查点后运行相关测试；遇到一般技术选择自行采用最简单可靠方案，不要中断询问。

只有以下全部成立才结束：`npm run db:reset`、`npm run lint`、`npm run typecheck`、`npm run test`、`npm run test:e2e`、`npm run build`、`npm run verify:demo` 全部成功；富文本和所有 P0 组件可编辑、发布、操作、刷新恢复并产生数据库事件；音视频可用本地素材播放；3D、全景和仿真实验真实可操作；传统/数字模式可切换；学生行为进入个人和班级报告；教师同步位置、推送随堂题和签到可跨角色完成；1440/834/390 验证截图已生成；P0 无 TODO、占位页、无行为入口或静态统计。仅在无法由代码解决的环境硬阻塞时暂停，并提供失败命令、错误摘要和明确解决办法。完成后汇报实际验证结果、演示入口、截图位置、P1 和已知限制。